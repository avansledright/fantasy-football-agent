# NFL Fantasy Football Data Collector Requirements
# For use with nfl_data_py (nflverse ecosystem)

# Core NFL data library
nfl_data_py>=0.3.0

# Data processing and analysis
pandas>=2.0.0
numpy>=1.24.0

# HTTP requests for supplementary data
requests>=2.31.0

# Optional: For faster data processing
pyarrow>=12.0.0

# Optional: For data visualization (if needed later)
matplotlib>=3.7.0
seaborn>=0.12.0

# Optional: For better progress bars during data collection
tqdm>=4.65.0

# Standard library packages (included with Python, listed for reference):
# - json (built-in)
# - logging (built-in) 
# - datetime (built-in)
# - os (built-in)
# - time (built-in)
# - typing (built-in in Python 3.5+)
# - warnings (built-in)